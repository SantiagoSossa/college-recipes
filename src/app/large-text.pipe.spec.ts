import { LargeTextPipe } from './large-text.pipe';

describe('LargeTextPipe', () => {
  it('create an instance', () => {
    const pipe = new LargeTextPipe();
    expect(pipe).toBeTruthy();
  });
});
